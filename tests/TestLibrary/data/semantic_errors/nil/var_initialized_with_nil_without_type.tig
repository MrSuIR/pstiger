let
    type Point = { x: int, y: int }
    function printPoint(p: Point) = (
        printi(p.x);
        print(", ");
        printi(p.y)
    )
    var point := nil
in
    point := Point{ x = 10, y = 12 };
    printPoint(point)
end