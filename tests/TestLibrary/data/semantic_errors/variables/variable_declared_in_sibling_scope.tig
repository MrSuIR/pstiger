/*
 Нельзя использовать переменную, объявленную в другой области видимости
 */
(
    let
      var x : int := 10
    in
      printi(x)
    end;
    let
      var y : int := 20
    in
      printi(x)
    end
)