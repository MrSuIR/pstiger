# Типы данных

> ⓘ Данный документ описывает только правила, которые будут реализованы на текущем этапе.

Этот документ описывает семантику типов языка Tiger.

## Предопределённые типы

В языке Tiger есть два предопределённых типа данных:

1. `int` — целочисленный тип
2. `string` — строка

Кроме того, выражения могут не возвращать никакого значения — в этом случае их нельзя размещать в контексте, где ожидается значение.

## Объявление новых типов

Можно объявлять как новые типы, так и синонимы существующих типов.

* Конструкция `type a = b` объявляет `a` как синоним типа `b`
* Конструкция `type a = array of b` объявляет `a` как массив, элементы которого имеют тип `b`
* Конструкция `type point = { x: int, y: int }` объявляет `point` как структуру с двумя полями целочисленного типа `x` и `y`

## Литералы

Виды литералов:

1. Литерал числа, например: `2025`
2. Литерал строки, например: `"Hello, world!"`
3. Литерал массива, например: `intArray[2] of 0` — создаёт массив типа `intArray` размером в 2 элемента, заполненный нулями
4. Литерал структуры, например: `point{ x = 10, y = 15 }` — создаёт структуру типа `point` с инициализацией полей `x` и `y` значениями 10 и 15 соответственно

## Операторы

- Для арифметических операторов `+`, `-`, `*`, `/` операнды должны быть целыми числами, результат — целое число
- Для логических операторов `&`, `|` операнды должны быть целыми числами, результат — целое число (1 или 0)
- Для операторов сравнения `<`, `>`, `<=`, `>=` операнды должны быть оба целыми числами либо строками, а результат — целое число (0 или 1)
- Для операторов сравнения `=` и `<>` допускаются любые операнды одинакового типа, а результат — целое число (0 или 1)

## Конструкции, не возвращающие значений

Следующие конструкции никогда не возвращают значения:

1. Присваивания — например, `a := b`
2. Выражения области видимости без вложенных выражений: `let ... in end`
3. Пустая последовательность выражений — `()`
4. Ветвления без ветки else — например, `if n > 0 then f(n - 1)`
5. Вызовы функций, для которых не объявлен возвращаемый тип — например, `function printLine(text: string) = print(text + "\n")`
6. Циклы while и for
7. Выражения break

## Массивы и структуры

- При доступе к элементу массива индекс должен находиться в диапазоне от 0 до n-1
- При доступе к полю структуры используемое имя поля должно совпадать с одним из имён полей в объявлении типа структуры

## Рекурсивные типы

- Допускается рекурсивное объявление типов.
- Допускается взаимная рекурсия объявлений типов, если последовательность объявлений этих типов находится в одной области видимости и не прерывается объявлениями переменных и функций.
- Каждая итерация рекурсивного объявления типов должна пройти через объявление структуры или массива. Другими словами, нельзя рекурсивно объявлять только синонимы типов.
